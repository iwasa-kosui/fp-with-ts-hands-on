# Session 00: 引き継ぎ

これは Session 00 開始時点の、意図的に壊れやすい予約管理のスナップショットです。

このディレクトリは Session 00 の開始スナップショットです。解答は `examples/session-01/src` にあります。

`src/legacy/` のコードと事故報告を読み、会計済みの来院を診察中へ戻せること、予約全体がログへ出ることを観察します。演習コマンドはありません。

```bash
pnpm --filter @fp-with-ts/clinic-session-00 test
```
