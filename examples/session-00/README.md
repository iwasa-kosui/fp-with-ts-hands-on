# Session 00: 引き継ぎ

これは、現行業務と既存システムを観察するための、意図的に壊れやすい予約管理のスナップショットです。

`src/legacy/` のコードと事故報告を読み、会計済みの来院を診察中へ戻せること、予約全体がログへ出ることを観察します。コード演習は行いません。

```bash
pnpm demo:00
```

`http://localhost:3000` でlegacyの操作と事故を再現できます。「デモを初期状態へ戻す」で同じ予約済み状態からやり直せます。
