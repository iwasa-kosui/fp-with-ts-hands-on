{
  "name": "@fp-with-ts/clinic-session-05",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "typecheck:exercise": "tsc --noEmit -p tsconfig.exercises.json",
    "test": "vitest run",
    "exercise": "vitest run --config vitest.exercises.config.ts"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "vitest": "^2.1.0"
  }
}
