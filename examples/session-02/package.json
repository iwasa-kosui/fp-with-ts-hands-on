{
  "name": "@fp-with-ts/clinic-session-02",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "typecheck:exercise": "tsc --noEmit -p tsconfig.exercise.json",
    "test": "vitest run",
    "exercise": "pnpm run typecheck:exercise && vitest run --config vitest.exercises.config.ts"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
