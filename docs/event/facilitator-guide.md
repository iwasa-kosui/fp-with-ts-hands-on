# ファシリテーター向けガイド

## イベント概要

- 開催日時: 2026年8月30日 15:00-18:30（210分）
- 体制: 5人1班、各班にTA 1名
- 対象: TypeScript の基本文法を読み書きできる初級から中級の参加者
- 題材: 動物病院の予約・カルテ管理システム
- 主線: 事前にセットアップしたローカル clone
- 退避先: S2〜S6はページの「ステップごとの解答」の `details`、S6は後続stepを含む全targetの完成ファイル、共通で WebContainer Playground

コーディングエージェントは推奨ですが必須ではありません。参加者が担うのは、事故から不変条件を言語化し、依頼文または実装メモへ変換し、型とテストで個人検証したうえで班内相互レビューを行うことです。

## テストと演習コマンドの契約

- `pnpm test`、`pnpm typecheck`、`pnpm build` は通常の健全性確認で、成功を期待します。
- S2〜S6 の開始スナップショットでは、`pnpm exercise:02`〜`pnpm exercise:06` が業務語彙を持つ `AssertionError` で意図的に失敗します。module-not-found、import error、設定エラーは意図した RED ではありません。
- 参加者が対象モジュールを直した後は、同じ exercise コマンドの成功を期待します。
- S0 は現状理解、S1 は担当者ごとの記録からドメインイベントを拾い、Excalidraw の患者別レーンへ置く班ワークです。どちらにも exercise コマンドはありません。
- S5 は同期 `Result` と `andThen`、S6 は副作用・イベント、`ResultAsync`、`andThrough`、1回の `store(event)` を扱います。

参加者ステップは S2=4件、S3=3件、S4=2件、S5=3件、S6=4件です。開始時の RED 数と同じではない出力や予期しない例外を見たら、先に環境または差分範囲を切り分けます。

## 当日のタイムテーブル

休憩30分とセッション内容180分を合わせて210分です。

| セッション | 分 | 経過 | 時刻 | 内容 |
| --- | ---: | --- | --- | --- |
| S0 | 10分 | 0:00-0:10 | 15:00-15:10 | 現行業務、画面操作、保存・ログと事故を対応付ける |
| S1 | 15分 | 0:10-0:25 | 15:10-15:25 | 並行する2つの来院から集約と未解決の責任を見つける |
| S2 | 30分 | 0:25-0:55 | 15:25-15:55 | current state: 6状態、許可された遷移、網羅性を扱う |
| S3 | 30分 | 0:55-1:25 | 15:55-16:25 | 値の意味: 用途の異なる識別子を型で区別する |
| S4 | 30分 | 1:55-2:25 | 16:55-17:25 | input: 外部入力の検証とPIIを扱う |
| S5 | 30分 | 2:25-2:55 | 17:25-17:55 | expected failures: 同期Resultと失敗経路を扱う |
| S6 | 30分 | 2:55-3:25 | 17:55-18:25 | output event/side effects: 保存と整合性境界を扱う |
| Final | 5分 | 3:25-3:30 | 18:25-18:30 | 講師による参照実装ツアー |

固定の休憩は次の30分です。

| 経過 | 時刻 | 分 | 内容 |
| --- | --- | ---: | --- |
| 1:25-1:55 | 16:25-16:55 | 30分 | 休憩。S4の開始時刻は動かさない |

## セッション内訳と運営切替時刻

分数の正規値は `apps/docs/src/sessions/catalog.ts` の `durationMinutes`、`timeBreakdown`、`adv`、`peerReview.minutes` です。このガイドの表は当日運営用の写しであり、文書契約テストで catalog と照合します。前節のタイムテーブルはセッション境界、この節はセッション内部の切替だけを扱います。数値が食い違った場合は catalog を正としてこの節を直し、二つの表を独立した正規値として運用しません。

| セッション | kind | brief | teach | exercise | review | 合計 |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| S0 | `orientation` | 4分 | 3分 | 0分 | 3分 | 10分 |
| S1 | `workshop` | 3分 | 4分 | 6分 | 2分 | 15分 |
| S2 | `exercise` | 4分 | 6分 | 13分 | 7分 | 30分 |
| S3 | `exercise` | 4分 | 6分 | 13分 | 7分 | 30分 |
| S4 | `exercise` | 4分 | 7分 | 12分 | 7分 | 30分 |
| S5 | `exercise` | 4分 | 3分 | 15分 | 8分 | 30分 |
| S6 | `exercise` | 4分 | 3分 | 15分 | 8分 | 30分 |
| Final | `reference` | 0分 | 4分 | 0分 | 1分 | 5分 |

- S0 の `brief` は業務説明、`teach` はガイド付き読解、`review` は班内共有です。相互レビューではありません。4+3+0+3=10分です。
- S1 の `exercise` は、担当者ごとの記録から拾ったドメインイベントをムギとソラのレーンへ置き、一貫性を保ちたい対象ごとに囲む6分です。急患優先後にムギの診察保留を誰が記録して受付へ伝えるかは「?」として残します。コード編集や exercise コマンドは行いません。3+4+6+2=15分です。
- S2〜S6 の `brief` は事故と RED、`teach` は技法の解説、`exercise` は ADV、`review` は班内相互レビューと末尾の記録です。それぞれ 4+6+13+7=30分、4+6+13+7=30分、4+7+12+7=30分、4+3+15+8=30分、4+3+15+8=30分です。
- Final の `teach` は講師ツアー、`review` は次の学習先の案内です。参加者レビューではありません。0+4+0+1=5分です。

S2〜S6 の `exercise` は、ADV の言語化・委譲・個人検証へ分けます。次の実時刻で声をかけて切り替えます。各行の6枠は、上表の `brief`、`teach`、ADV 3枠、`review` に対応します。

| セッション | brief | teach | 言語化 | 委譲 | 個人検証 | review／記録 |
| --- | --- | --- | --- | --- | --- | --- |
| S2 | 15:25-15:29（4分） | 15:29-15:35（6分） | 15:35-15:37（2分） | 15:37-15:46（9分） | 15:46-15:48（2分） | 15:48-15:55（7分） |
| S3 | 15:55-15:59（4分） | 15:59-16:05（6分） | 16:05-16:07（2分） | 16:07-16:16（9分） | 16:16-16:18（2分） | 16:18-16:25（7分） |
| S4 | 16:55-16:59（4分） | 16:59-17:06（7分） | 17:06-17:08（2分） | 17:08-17:16（8分） | 17:16-17:18（2分） | 17:18-17:25（7分） |
| S5 | 17:25-17:29（4分） | 17:29-17:32（3分） | 17:32-17:34（2分） | 17:34-17:44（10分） | 17:44-17:47（3分） | 17:47-17:55（8分） |
| S6 | 17:55-17:59（4分） | 17:59-18:02（3分） | 18:02-18:04（2分） | 18:04-18:14（10分） | 18:14-18:17（3分） | 18:17-18:25（8分） |

ADV の検算は S2=2+9+2=13分、S3=2+9+2=13分、S4=2+8+2=12分、S5=2+10+3=15分、S6=2+10+3=15分です。個人検証では exercise を GREEN にして、後述する現在snapshot限定の差分とリポジトリ全体の状態を確認し、`review／記録` の末尾1分で全員がシートへ記入します。

## 講師の共通台本

S2〜S6 は次の順序を崩しません。

1. 要求と事故を読み、「何が壊れたか」を業務の言葉で確認する。
2. exercise コマンドを実行し、意図した assertion RED を見る。
3. 参加者が「〜が〜できない」の形で不変条件を1文にする。
4. その1文を依頼文へ入れる。エージェントを使わない参加者は実装メモとして使う。
5. 対象モジュールだけを変更し、同じコマンド、型、現在snapshot限定の差分、リポジトリ全体の状態で個人検証する。
6. TA が選んだ1〜2名の差分を班で相互レビューし、全員がレビュー観点シートへ1行書く。

S0 では現状を読み、S1 では3人の担当者の記録からドメインイベントを拾い、ムギとソラのレーンへ置きます。コマンドとアクターを添えて集約を見つけ、依頼者を決められないムギの診察保留には「?」を置きます。どちらも修正と exercise 実行を行いません。Final は講師が `examples/final/src/useCase/startExaminationUseCase.ts` を入口に5分で案内するだけです。参加者へ `examples/final` のセットアップ、編集、Playground 操作を求めません。

## S1 の Excalidraw 運用

S1 の班ワークはページ上ではなく Excalidraw で行います。班数分のリンクを事前に用意する案は採りません。リンクが外部に残り続けるためです。TA は S1 のブリーフィング3分の間に次を終えます。

1. excalidraw.com でテンプレート `docs/event/session-01-event-storming.excalidraw` を開く。
2. 画面右上から Live collaboration を開始する。
3. 発行されたリンクを班へ共有する。

参加者が担当者の記録を読み終える前にリンクを配り終える想定です。テンプレートには講師実演1件、ムギとソラの空きレーン、ドメインイベント・コマンド・アクター・集約・未解決の問いの記入例、手順4段を含みます。

## セッション間の差分基準と引き継ぎ

相互レビューで表示するのは、現在の開始snapshotディレクトリだけです。前のセッションの未commit差分が残っていても現在のレビューへ混ざらないよう、次を使います。

| セッション | 開始snapshot | 差分確認 |
| --- | --- | --- |
| S2 | `examples/session-02` | `git diff --stat -- examples/session-02` |
| S3 | `examples/session-03` | `git diff --stat -- examples/session-03` |
| S4 | `examples/session-04` | `git diff --stat -- examples/session-04` |
| S5 | `examples/session-05` | `git diff --stat -- examples/session-05` |
| S6 | `examples/session-06` | `git diff --stat -- examples/session-06` |

各回で `git status --short` も実行し、現在snapshot以外に想定外pathが増えていないかをTAが確認します。これはリポジトリ全体の異常検知であり、レビュー対象の差分量を判定するコマンドではありません。

レビュー終了後はシートへの記録を確認し、次のセッションページとexerciseコマンドへ進み、差分確認のpathだけを次のsnapshotへ切り替えます。前のセッションの未commit差分を消すための reset、stash、commit は要求しません。想定外pathがあっても自動で消さず、参加者が残したい作業を確認してから [トラブルシューティング](./troubleshooting.md) の限定手順を使います。

## Checkpoint

### 0:10 — S0 を終えて S1 へ進む

- 2件の事故（二重請求、PII流出）と legacy の該当行を対応づけられるか確認する。
- 実装を先回りして直さず、S1で並行する2つの来院を患者別レーンへ置き、業務イベントを集約ごとにまとめることを共有する。
- TA が Excalidraw のテンプレートを開き、Live collaboration を開始する準備をする。

### 0:25 — S1 を終えて S2 へ進む

- 7件のドメインイベントがムギとソラのレーンへ置かれ、予約とカルテが患者ごとの集約として分かれているか確認する。
- ムギの診察保留を誰が予約へ記録し、受付へ伝えるかが「?」として残っているか確認する。
- S2以降はカードの未解決リスクを1回に1つずつ実装することを共有する。
- [相互レビュー進行カード](./peer-review-card.md)の約束事5点を読み上げる。5回で全員を最低1回選ぶ計画をTAが記録欄へ立てる。

### 0:55 — S2 を終えて S3 へ進む

- 6状態と許可された遷移、診察結果記録前の会計禁止、キャンセル理由、`assertNever` の役割を確認する。
- 4ステップを完走できない参加者は、ページの「ステップごとの解答」の `details` を開いて同じ型を読める状態にする。
- 相互レビュー7分は落とさない。時間超過時は2人目の比較枠2分を落とし、1名の5分版へ切り替える。

### 1:25 — S3 を終えて休憩へ入る

- 用途の異なる識別子が互いに代入できないこと、予約の6状態と遷移の両方へ型が通っていることを確認する。
- 参加者ステップは catalog の3件である。時間が足りなければ、末尾の型テストを講師デモへ切り替える。
- 相互レビュー7分は残し、必要な場合だけ2人目の比較枠を落とす。休憩30分を削らない。

### 1:55 — 休憩から S4 へ戻る

- 不正JSONが `err` になり、連絡先が JSON と inspect の両方で伏せられることを確認する。
- 参加者ステップは catalog の2件である。固定の「Step 3/4」が存在する前提で進行しない。
- brand と `Sensitive` が1行に並ぶ理由を、識別のための区別とログ漏えいの防止に分けて説明する。

### 2:55 — S5 を終えて S6 へ進む

- `AppointmentNotFound` と `InvalidAppointmentState` が例外文言ではなく同期 `Result` の値として返ることを確認する。
- S5の3ステップが終わらない場合は `ensure*` の解答と `andThen` pipeline を順に開き、失敗後に遷移しない理由を読ませる。
- 後半の残り時間を確認し、S6が押す見込みなら S6のステップ3（`ResultAsync` と `andThrough`）を講師デモにする。S6の相互レビューは残し、次に2人目枠、最後にFinalの説明量を調整する。

## 班とTAの運用

オープニングでエージェント有無を申告してもらい、可能な範囲で保持者と非保持者を同じ班にします。TA は保持者0名の班を把握します。

委譲時間の巡回には2つの目的があります。

1. 赤の数、型エラー、モジュール外差分から詰まりを発見し、エージェントを使わない参加者を優先して支援する。
2. [相互レビュー進行カード](./peer-review-card.md)の選定基準に沿って、学びの出る1〜2名の差分を選ぶ。

相互レビューの末尾1分では、自分の差分が選ばれなかった参加者も [レビュー観点シート](./review-sheet.md) の「型で守れなかった残り」を書きます。2名を選ぶ回を4回以上確保し、5回を通して班の全員が最低1回は選ばれるよう、カード裏面の記録欄で調整します。

## エージェントを使わない参加者の退避

- S2〜S5はページの「ステップごとの解答」の `details` を1件ずつ開き、次スナップショットの抜粋を写して理由を読む。
- S5は「完成ファイルの解答例」の `details` を開く。各解答は後続stepを含む完成ファイルなので、表示された全target fileを反映してから同じexerciseを実行する。1stepずつの個別GREENは約束しない。
- `配布コードを読む` の Code Explorer は、実ファイルの注目行を読むためのガイドとして使う。
- `型で閉じる` の Playground は WebContainer 上で編集とコマンド実行ができる説明用・退避用環境として使う。主線にはせず、会場ネットワークに依存させない。
- Playground が起動しない場合はローカル clone へ戻る。新しいAPIキーや特定のエージェント製品は必要ない。
- 相互レビューではモジュール全体ではなく、全員が到達している1ステップ・1宣言を対象にする。解答を写した差分なら「なぜ解答はこの形か」「別の形だと何が壊れるか」と問う。

Code Explorer の専用URLはありません。各セッションの `配布コードを読む` と `型で閉じる` に埋め込まれた実体を使います。

## 事前リハーサル手順

自動検証の実測結果はP4のリハーサル記録へ残します。このガイドでは実行順と期待だけを定めます。

1. 依存関係を事前に取得し、`pnpm typecheck`、`pnpm test`、`pnpm build` が成功することを確認する。これらはS0〜S7のWebアプリも検査する。
2. `pnpm exercise:02`〜`pnpm exercise:06` を個別に実行し、開始スナップショットとして non-zero の assertion RED になり、参加者ステップが S2=4、S3=3、S4=2、S5=3、S6=4 に対応することを確認する。
3. `pnpm --filter @fp-with-ts/clinic-session-07 test` が S2〜S6 の全回帰を含めて成功することを確認する。
4. `pnpm demo:00` から `pnpm demo:07` を一つずつ起動し、`http://localhost:3000` の初期表示、通常操作、reset、未実装ダイアログを確認する。切り替える前に現在のサーバーを `Ctrl+C` で終了する。
5. 5人相当の差分で7分版を通し、問い1で沈黙しないか、2人目の比較まで収まるかを計測する。成功済みとは記録せず、実測結果をP4へ残す。
6. エージェントを使わない条件で、S2〜S5の解答 `details` またはS6の全target完成ファイルを開き、委譲時間内に進めるかを1本計測する。

スマホ表示を確認するときは開発サーバーを `0.0.0.0` で待ち受けます。現行の `pnpm dev` はこの設定を持ちます。表示された実ポートに対して `mobile-preview-url <port>` を実行し、返されたURLを使います。明示的な依頼がない限り公開トンネルは使いません。

## 会場表示

班ごとの外部ディスプレイ、HDMI、USB-C変換アダプタ、電源の有無と台数は未確認です。利用できる場合は第一案として使います。利用できない場合は、選ばれた参加者のラップトップを島の中央へ向け、エディタのフォントを一時的に拡大し、対象を1ファイル・20行以内へ絞ります。設備を確認済みとは案内しません。

## 30日後フォローアップ

[30日後フォローアップ運用手順](./follow-up-30-days.md) に4つの設問、主催者/運営責任者、D+30の一斉連絡、回答締切、匿名集計を定義しました。設問と送付方法は準備済みです。実送付は開催後のため未実施です。

問題の切り分けは [トラブルシューティング](./troubleshooting.md) を使います。
